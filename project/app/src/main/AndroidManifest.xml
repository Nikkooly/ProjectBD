<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bstu.fit.yarmolik.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChangeTeacher"/>
        <activity android:name=".BookAudit" />
        <activity android:name=".adminPanel.ChangeAudit" />
        <activity android:name=".adminPanel.ListItemActivity" />
        <activity android:name=".adminPanel.AuditActivity" />
        <activity android:name=".adminPanel.Admin" />
        <activity android:name=".startPage.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".startPage.Registration" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>